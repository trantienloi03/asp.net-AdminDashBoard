@model SV21T1020484.DomainModels.Supplier
@{
}
<div class="box box-danger">
    <div class="box-body">
        <form action="/Supplier/Delete/@Model.SupplierID" method="post">
            <div class="form-group">
                <label>Tên nhà cung cấp</label>
                <p class="form-control-static">@Model.SupplierName</p>
            </div>
            <div class="form-group">
                <label>Tên giao dịch</label>
                <p class="form-control-static">@Model.ConTactName</p>
            </div>
            <div class="form-group">
                <label>Tỉnh thành</label>
                <p class="form-control-static">@Model.Province</p>
            </div>
            <div class="form-group">
                <label>Địa chỉ</label>
                <p class="form-control-static">@Model.Address</p>
            </div>
            <div class="form-group">
                <label>Điện thoại</label>
                <p class="form-control-static">@Model.Phone</p>
            </div>
            <div class="form-group">
                <label>Email</label>
                <p class="form-control-static">@Model.Email</p>
            </div>
            <div class="form-group text-right">
                @if (SV21T1020484.BusinessLayers.CommonDataService.InUseSupplier(Model.SupplierID) == false)
                {
                     <button type="submit" class="btn btn-danger">
                        <i class="fa fa-trash">Xác nhận xóa</i>
                    </button>
                }
                
                <a href="~/Supplier" class="btn btn-default" >Quay lại</a>
            </div>

        </form>
    </div>
</div>
